import ecuadorPathDataJson from "../../legacy-app/ecuador-path-data.json";
import type { EcuadorPathDataset } from "../lib/types";

export const ecuadorPathData = ecuadorPathDataJson as EcuadorPathDataset;
